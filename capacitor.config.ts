import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.ahmed_develop.fanatico',
  appName: 'fanatico',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
